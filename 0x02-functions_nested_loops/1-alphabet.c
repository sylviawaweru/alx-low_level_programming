#include"main.h"

/**
 * print_alphabet - prints the alphabet, in lowercase, followed by a new line.
 *
 *prints lower case
 *
 * Return: 0 (success)
 */

void print_alphabet(void)
{
	char x;

	for (x = 'a'; x <= 'z'; x++)
	{
		putchar(x)
	}
	putchar('\n')
}
