#include<stdio.h>
#include"main.h"

/**
 * _strcat - concatenates two strings.
 * @dest: the destination string
 * @src: the source string
 *
 * Return: *ptr pointer to the resulting string dest
 */
char *_strcat(char *dest, char *src)
{
	char src[10] = "Hello";
	char dest[10] = "world";

	char *ptr

		strncat(src, dest);
	return (*ptr);
}
